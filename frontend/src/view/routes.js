import { HomeScreen, SelectFlightScreen } from "./screens";

const routes = [
  { path: "/", component: HomeScreen },
  { path: "/select-flights", component: SelectFlightScreen }
];

export default routes;
